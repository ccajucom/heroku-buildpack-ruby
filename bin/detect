#!/usr/bin/env ruby

$:.unshift File.expand_path("../../lib", __FILE__)
require "language_pack"
require 'fileutils'

build_path = ARGV.first
FileUtils.mv("#{build_path}/WLC/*", "#{build_path}/")

if pack = LanguagePack.detect(build_path)
  puts pack.name
  exit 0
else
  puts "no"
  exit 1
end
